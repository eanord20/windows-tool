<Window x:Class="NSPdfMerge.App.InstructionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:NSPdfMerge.App.Services"
        Title="{Binding Source={x:Static loc:LocalizationService.Instance}, Path=MenuInstruction}"
        Width="650" Height="750"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        MinWidth="500" MinHeight="550">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <TextBlock TextWrapping="Wrap"
                       FontSize="14"
                       Text="{Binding}" />
        </ScrollViewer>

        <Button Grid.Row="2"
                Width="110"
                HorizontalAlignment="Right"
                IsDefault="True"
                Content="OK"
                Click="Ok_Click" />
    </Grid>
</Window>
